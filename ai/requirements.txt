# conda create -n ai python=3.12
# pip install -r requirements.txt

aiohttp
uvicorn
starlette

# CPU whisper on CTranslate2. Pulls in ctranslate2, tokenizers, onnxruntime
# (the Silero VAD) and av (decoding) -- no torch, no transformers, and no
# system ffmpeg. >=1.1 is what knows the 'large-v3-turbo' name.
faster-whisper>=1.1
