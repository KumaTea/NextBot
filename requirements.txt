# common
aiohttp
beautifulsoup4
requests
uvloop; sys_platform != 'win32'

# telegram
telethon
# optional, but a large speed-up for MTProto crypto:
cryptg

# X mirror -- a maintained fork of twikit; imports as `twikit`.
# The bot still starts (with the mirror disabled) if this is missing.
twifork

# gemini, via the openai-compatible endpoint
openai>=1.0.0
